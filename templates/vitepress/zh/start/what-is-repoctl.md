---
title: 什么是 repoctl？
description: repoctl 是面向 pnpm 与 Turborepo monorepo 的官方任务型 CLI，用于初始化、诊断和维护仓库。
---

# 什么是 repoctl？

repoctl 把仓库约定变成可重复执行的初始化、诊断、创建、校验、报告和发布命令。

```bash
pnpm add -D repoctl
pnpm exec repo init
pnpm exec repo doctor
```

继续阅读[安装 repoctl](/zh/start/install-repoctl)或[命令速查](/zh/reference/commands)。
